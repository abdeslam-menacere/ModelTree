# ADR 0003: A deliberate collision for CI verification

NOT A DECISION RECORD. This file exists for exactly one commit, to prove the
adr-numbers workflow can fail on a real duplicate number rather than passing
because it never found the files it looks for. The next commit removes it.

